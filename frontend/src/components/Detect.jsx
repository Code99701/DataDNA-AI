import React from 'react';

const Detect = () => {
  return (
    <div className="detect-container">
      <h2>Detect</h2>
      <p>Detection component placeholder.</p>
    </div>
  );
};

export default Detect;
